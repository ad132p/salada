# ~/.config/containers/systemd/salada-app-db.pod
[Pod]
Description=Salada Go App and PostgreSQL Pod
PodName=salada-main-pod # A unique name for your Podman pod

# Expose the Go application's port (8080) from the pod to the host's 8080
# Format: host_port:container_port
PublishPort=8080:8080

# You can optionally specify a network here, or let Podman use the default bridge
# Network=bridge

# Assign static IPs, though for local communication in the pod, localhost is used
# No need to expose DB port here, as it's only for internal pod communication
